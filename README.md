# 훗카이도 남부 가족여행 웹앱

## 실행
```bash
cd hokkaido-trip-app
python3 -m http.server 8787
```
브라우저에서 `http://localhost:8787` 접속.

## 모바일에서 사용
- Safari/Chrome에서 주소 열기
- 홈 화면에 추가(Add to Home Screen)
- 한 번 열어두면 기본 정보는 오프라인에서도 표시됨

## 파일
- `index.html`: 일정/맛집/아기동선 팁
- `manifest.json`: 앱 메타
- `sw.js`: 오프라인 캐시
